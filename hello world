git
>> laptop to github.com
>>{{ git status }}of the folder on the laptop
>> {{git init }}
>>create a file in the folder
>>for tracking the file do {{"git add file_name"}}
>>for commiting the file do  {{git commit -m "message"}}
>>for knowing the previous commit {{git log}}
>> for knowing what is upload on the previous commit{{git show commit_number}}
